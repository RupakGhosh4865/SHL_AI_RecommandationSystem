# ===================================
# Minimal Docker Requirements
# Only essential packages - no bloat
# ===================================

# Core (already in base image)
pandas==2.1.1

# Google Gemini (Lightweight LLM)
google-generativeai==0.3.2

# Embeddings (Local - ~80MB download)
sentence-transformers==2.2.2

# Vector Database (Lightweight)
chromadb==0.4.18

# Utilities
python-dotenv==1.0.0
tqdm==4.66.1